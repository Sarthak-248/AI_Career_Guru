{
  "name": "ai-career-coach-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node express-server/server.js",
    "start:client": "cd client && npm run dev",
    "start:server": "cd express-server && npm run dev",
    "build": "cd client && npm install && npm run build && cd ../express-server && npm install && npm run prisma:generate",
    "install:all": "cd client && npm install && cd ../express-server && npm install",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  }
}
